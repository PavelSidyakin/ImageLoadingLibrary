package com.image_loading_library.impl

import android.widget.ImageView
import com.image_loading_library.ImageDownloader
import javax.inject.Inject

internal class ImageDownloaderImpl
    @Inject
    constructor(): ImageDownloader {

    override fun load(imageView: ImageView) {
    }
}